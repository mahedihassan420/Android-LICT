<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/activity_main"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:paddingBottom="@dimen/activity_vertical_margin"
    android:paddingLeft="@dimen/activity_horizontal_margin"
    android:paddingRight="@dimen/activity_horizontal_margin"
    android:paddingTop="@dimen/activity_vertical_margin"
    tools:context="com.example.ovi.recycler_grid_staggeredviewdemo.MainActivity">

    <Button
        android:id="@+id/stagger"
        android:layout_width="@dimen/size150dp"
        android:layout_height="wrap_content"
        android:text="@string/stagger"
        android:textStyle="bold"
        android:layout_marginBottom="125dp"
        android:layout_alignParentBottom="true"
        android:layout_centerHorizontal="true" />

    <Button
        android:id="@+id/grid"
        android:layout_width="@dimen/size150dp"
        android:layout_height="wrap_content"
        android:text="@string/grid"
        android:textStyle="bold"
        android:layout_above="@+id/recycler"
        android:layout_alignLeft="@+id/recycler"
        android:layout_alignStart="@+id/recycler"
        android:layout_marginBottom="24dp" />

    <Button
        android:id="@+id/recycler"
        android:layout_width="@dimen/size150dp"
        android:layout_height="wrap_content"
        android:text="@string/recycler"
        android:textStyle="bold"
        android:layout_marginBottom="20dp"
        android:layout_above="@+id/stagger"
        android:layout_alignLeft="@+id/stagger"
        android:layout_alignStart="@+id/stagger" />
</RelativeLayout>
